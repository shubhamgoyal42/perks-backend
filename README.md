# perks-backend
The backend for perks app; a project for ETHGlobal Online 2023

## Setup

Use the fullScript.ts to deploy the contracts and make some test transactions.

```
export PRIVATE_KEY=<private key>
npx hardhat run scripts/fullScript.ts --network mumbai
```